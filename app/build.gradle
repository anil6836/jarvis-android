plugins {
    id 'com.android.application'
}

def runNumber = (System.getenv('GITHUB_RUN_NUMBER') ?: '1').toInteger()

android {
    namespace 'com.anil.jarvis'
    compileSdk 34

    defaultConfig {
        applicationId 'com.anil.jarvis'
        minSdk 26
        targetSdk 34
        versionCode runNumber
        versionName "1.0.${runNumber}"
        ndk {
            // Most Android phones; keeps the download small.
            abiFilters 'arm64-v8a', 'armeabi-v7a'
        }
    }

    signingConfigs {
        release {
            storeFile file('jarvis.keystore')
            storePassword 'jarvis-anil'
            keyAlias 'jarvis'
            keyPassword 'jarvis-anil'
        }
    }

    buildTypes {
        release {
            minifyEnabled false
            signingConfig signingConfigs.release
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }

    lint {
        abortOnError false
        checkReleaseBuilds false
    }
}

dependencies {
    // On-device "Hey Jarvis" wake word (openWakeWord models run with ONNX Runtime)
    implementation 'com.microsoft.onnxruntime:onnxruntime-android:1.20.0'
}
